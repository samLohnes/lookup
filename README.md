# Satellite Visibility

Local, research-grade satellite pass prediction and visualization.

**Status:** M1 in progress — pure Python engine.

See `docs/superpowers/specs/2026-04-20-satellite-visibility-design.md` for the full design.

## Development (M1)

```bash
uv venv
source .venv/bin/activate
uv pip install -e ".[dev]"
pytest
```

## Quick demo

```bash
python scripts/demo.py
```
