/** Placeholder while the full cinematic layout is under construction (M6).
 *  Task 23 of the M6 plan replaces this with the real composed layout. */
export function CinematicLayout() {
  return (
    <div className="fixed inset-0 grid place-items-center bg-bg text-fg-muted">
      <div className="text-center">
        <div className="text-sm uppercase tracking-wider mb-2">Cinematic mode</div>
        <div className="text-xs">Under construction — toggle to Research from the top-right.</div>
      </div>
    </div>
  );
}
